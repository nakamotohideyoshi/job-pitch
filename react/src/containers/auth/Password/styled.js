import styled from 'styled-components';
import { Form } from 'antd';

export default styled(Form)`
  max-width: 300px;
  width: 100%;

  label {
    color: #262626;
    line-height: 40px;
  }

  button {
    width: 100%;
  }
`;
