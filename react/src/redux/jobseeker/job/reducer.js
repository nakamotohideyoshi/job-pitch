import * as C from './constants';

const initialState = {
  jobs: null,
  errors: null
};

export default function reducer(state = initialState, action = {}) {
  switch (action.type) {
    default:
      return state;
  }
}
